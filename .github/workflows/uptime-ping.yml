name: Temporary Render uptime ping

on:
  workflow_dispatch:
  schedule:
    - cron: "*/5 * * * *"

jobs:
  ping:
    name: Ping Render health endpoints
    runs-on: ubuntu-latest
    timeout-minutes: 5
    if: ${{ vars.SIVAN_UPTIME_PING_ENABLED != 'false' }}
    steps:
      - uses: actions/checkout@v5
      - name: Ping WhatsApp bot and backend
        run: bash scripts/uptime-ping.sh
        env:
          UPTIME_PING_URLS: ${{ vars.SIVAN_UPTIME_PING_URLS || 'https://whatsapp-bot-ix7t.onrender.com/api/health,https://sivan-escrow-agent.onrender.com/api/health' }}
          UPTIME_PING_TIMEOUT_SECONDS: ${{ vars.SIVAN_UPTIME_PING_TIMEOUT_SECONDS || '25' }}
          UPTIME_PING_ATTEMPTS: ${{ vars.SIVAN_UPTIME_PING_ATTEMPTS || '3' }}
          UPTIME_PING_RETRY_DELAY_SECONDS: ${{ vars.SIVAN_UPTIME_PING_RETRY_DELAY_SECONDS || '15' }}
          UPTIME_PING_STRICT: ${{ vars.SIVAN_UPTIME_PING_STRICT || 'false' }}
